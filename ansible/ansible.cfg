[defaults]
inventory         = inventory/hosts.yml
roles_path        = roles
# Usuário e chave: ajuste no seu config.yml (ansible_user) se diferente do padrão.
private_key_file  = ~/.ssh/id_ed25519
host_key_checking = false

[privilege_escalation]
become        = true
become_method = sudo
become_user   = root
