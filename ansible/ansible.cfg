[defaults]
inventory         = inventory/hosts.yml
roles_path        = roles
remote_user       = pi
private_key_file  = ~/.ssh/id_ed25519
host_key_checking = false
vault_password_file = .vault_pass   # create this file locally, never commit

[privilege_escalation]
become      = true
become_method = sudo
become_user = root
