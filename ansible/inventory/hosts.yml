all:
  hosts:
    rpi5:
      # ansible_host e ansible_user vêm do seu config.yml:
      #   ansible-playbook playbook.yml -e @config.yml
      # (mantido genérico de propósito — nada de IP/host pessoal versionado)
