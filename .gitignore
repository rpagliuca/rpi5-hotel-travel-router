# Config local com seus valores/segredos (NUNCA comitar). Use config.example.yml.
ansible/config.yml
config.yml

# (legado) Ansible vault — não é mais usado, mas mantido ignorado por segurança
.vault_pass
*.vault_pass
ansible/inventory/group_vars/all/vault.yml

# SSH keys
*.pem
*.key
id_*

# Editor
.vscode/
.idea/
*.swp
*~

# OS
.DS_Store

# Tailscale auth keys (never commit)
tailscale_authkey*
